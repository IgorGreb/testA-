import 'package:flutter/material.dart';

Text apiTextInput = const Text('Set valid API base URL to fetch tasks',
    style: TextStyle(fontSize: 16));
    
Text buttonText = const Text('Start counting process',
    style: TextStyle(fontSize: 16,color: Colors.black));